---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: yes
    cache_valid_time: 3600
  become: yes

- name: Install baseline packages
  ansible.builtin.apt:
    name:
      - curl
      - vim
      - git
      - htop
    state: present
  become: yes